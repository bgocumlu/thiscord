{
  "name": "@thiscord/desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/main.cjs",
  "scripts": {
    "build": "node scripts/build.mjs",
    "watch": "node scripts/build.mjs --watch",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "tsx --test tests/*.test.mjs"
  },
  "devDependencies": {
    "@types/node": "^24.12.3",
    "electron": "^39.8.8",
    "esbuild": "^0.28.1",
    "typescript": "~6.0.2"
  }
}
